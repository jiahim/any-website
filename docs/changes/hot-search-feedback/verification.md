# 热门搜索与赞踩验收记录

本地交付结论：Ready。发布结论：Inconclusive，生产迁移和生产环境验收不在本次范围。

工作副本：`/Users/xiexin/project/any-website-hot-search-feedback`。

## 范围
用户已确认：历史数据不截断、30 天半衰期、不使用 Linear、热门搜索替换原排行榜、浮标加入可切换和取消的赞踩。不包含提交、PR、部署或生产数据库迁移。

分支：`codex/hot-search-feedback`，基准 `b9c8828`。

## 实现
- 独立的网站热词与社会热词区域：各最多 12 条，各自排序、刷新、加载和空态；不混排、不互相补位。显示来源及热词抓取时间，去掉名次和累计次数。
- 站内从所有保留的真实日志取每个路径/访客的最新活动，30 天半衰期；不使用 TrendingSearch.count 排序。初始脚本不再注入虚拟搜索。
- 质量修正采用 Beta(5,5) 平滑并限定在 0.8～1.2；每位匿名访客对一个词最多贡献一个票单位。
- 完整 SSE 输出后服务端创建实例；仅实例所属匿名访客可评价。重复 PUT 设置绝对状态，取消=0，切换覆盖原状态。
- 标签页缓存当前完成实例与 HTML，最长 30 分钟、1MB；刷新恢复同一实例与服务端评价。重新生成产生新实例，不继承旧评价。
- 百度读取固定公开地址，无 API Key；30 分钟缓存，24 小时硬过期，失败后 60 秒退避。数据库保存快照，数据库不可用时允许进程内缓存降级。

## 产品验收（Deep）
环境：独立本地 PostgreSQL `127.0.0.1:55439/aw_hot_search`；网站 `127.0.0.1:3147`。浏览器为 Codex 内置浏览器。模型响应由明确的测试 preload 替换为固定 HTML，不调用付费模型；百度数据源为真实公开页面。

| 关注点 | 状态 | 证据 | 限制 |
|---|---|---|---|
| 首页真实入口 | passed | “网站热词”与“社会热词”独立展示；本地验收站内 1 条、社会 12 条，分别刷新成功，无互相补位 | 未在生产地区验证上游长期可用性 |
| 热门词→生成 | passed | 点击“次抛衣”走红打开新标签页，完整 HTML 渲染后出现浮标 | 页面正文为测试模型内容 |
| 点赞/取消/点踩/切换 | passed | 浏览器 aria-pressed 与提示正确；真实 API 连续 8 次设置/取消/切换，GET 与写入一致 | 匿名身份不等于账号认证 |
| 刷新恢复 | passed | 点赞后刷新同一页面，恢复“取消点赞”选中态；不需要重新评价 | 30 分钟/1MB 缓存上限，禁用存储则重新生成 |
| 写入失败及恢复 | passed | 停止本任务测试 DB，点踩失败后回退到原点赞并提示“评价未保存”；恢复 DB 后重试成功 | 不影响生产数据库 |
| 实例权限与幂等 | passed | 无 cookie、其他 visitor cookie 返回 404；跨站 Origin 返回 403；重复 PUT 不累加 | 清除 cookie 仍可创建新匿名身份 |
| 手机浮标 | passed | 390×844 可见全部评价与下载按钮，拖拽到左侧后自动收起、点击展开 | 使用固定 HTML，未评价生成正文的设计质量 |
| 下载回归 | passed | 点击后显示“页面已保存到本地”，保持原下载入口；本地数据库记录下载 | 浏览器下载目录未作为交付物 |
| 首页手机布局 | passed | 生产模式 390×844；DOM innerWidth 与 scrollWidth 均为 390，来源与时间可见，无横向溢出 | — |
| 线上模型/数据库/部署 | not-tested | 未执行生产操作 | 上线前应用增量 SQL 并做部署环境验收 |

## 自动化与数据验证
- `node --experimental-strip-types --test tests/*.test.ts`：11 个聚焦测试，涵盖衰减、去重、demo 排除、平滑评价、取消、中文热词解析、缓存硬过期、SSE 完整性和持久化失败。
- `tests/api-integration.mjs`：真实本地 HTTP + PostgreSQL，含合法浏览器 Origin、归属、状态转换、并发搜索写入。
- 并发提交 5 个相同搜索后，SQL 确认 `user_search_logs` 仅 1 行。
- `tests/migration-uat.sql`：在独立 schema 从旧结构运行增量 SQL 两次，确认保留原日志并建立票值约束。
- TypeScript、改动文件 ESLint、`git diff --check`、OpenSpec strict 校验通过。
- 构建：常规 `next build` 因既有 Google Fonts 下载 ECONNRESET 失败；使用 curl 下载的同版官方 CSS 与三个原字体文件，通过 Next 的字体响应缓存入口补充构建，exit 0；项目字体配置未修改。随后 `next start` 生产模式下的完整接口冒烟检查通过。
- 字体缓存仅位于 `/private/tmp/any-website-font-cache`，构建变量 `NEXT_FONT_GOOGLE_MOCKED_RESPONSES` 指向该目录的 responses.cjs；这里使用真实官方字体内容，不是伪造字体。部署仍须保证构建网络可访问 Google Fonts。

## 上线前操作（本次未执行）
1. 对目标数据库执行 `prisma/changes/20260914-hot-search-feedback.sql`；该项目此前使用 db:push，因此提供独立的增量 SQL，而非假设存在 Prisma migration 基线。
2. 常规安装/构建会生成新的 Prisma Client；确认部署环境可获取 Google 字体及百度热榜。
3. 验证真实模型生成完整页面可评价、赞踩保存和刷新恢复。匿名投票只是初版质量信号，不宣称能防止换身份刷票。
4. 回滚时恢复原应用版本即可，新表可以保留，无需删除历史搜索数据。

## 复现本地 UAT
测试脚本需要支持 TypeScript stripping 的 Node（本次使用 Node 24）。用隔离库配置 `.env.local`，执行 `prisma db push`，然后：

```sh
AW_LOCAL_UAT=1 NODE_OPTIONS="--import $(pwd)/tests/mock-provider.mjs" pnpm dev --hostname 127.0.0.1 --port 3147
node tests/api-integration.mjs
```

模型 preload 仅用于本地测试，不属于应用运行时入口，不应在生产设置这些测试环境变量。

## 本地辅助资源
当前保留 3147 端口的验收预览和 55439 端口的专用数据库，预览模型为固定样例；原工作目录保持未修改。未提交、未推送、未创建 PR。

## 2026-09-14 来源拆分纠正

- 用户明确区分社会热词与网站热词，移除原来的混排和补位逻辑；站内默认请求仅返回 local，社会请求 source=social 仅返回 baidu。
- 11 项聚焦测试、TypeScript、修改文件 ESLint、生产构建通过（复用上述官方字体缓存）。
- 生产模式接口验收通过：来源隔离、空网站分类无外部补位、无效来源 400；原赞踩 8 次状态转换回归通过。
- 实际浏览器验证：首页两个独立标题、来源和刷新入口；390×844 手机视口无横向溢出，已恢复默认视口。
- 本地预览 http://127.0.0.1:3147/ 已更新；仍为隔离数据库和生成样例环境，未发布。

## 2026-09-14 时间权重纠正
- 用户明确时间是权重，不是范围；取消搜索和评价的 30 天过滤，采用 30 天半衰期，真实旧词仍可入榜。
- 取消 UserSearchLog 按 30 天删除的清理逻辑；SearchRecord 的原始 IP/UA 仍按原有 90 天清理。旧任务已经删除的日志无法还原，不使用累计虚拟数填补。
- 先失败证据：历史参与/历史评价两项测试在修改前失败；修改后 12 项测试全部通过。
- ESLint 与生产构建（含类型检查）通过。独立本地数据库跨层验收通过：120 天前记录在执行清理后仍保留，GET /api/trending 返回该候选；验收记录已清理。
- 真实浏览器首页文案更新为“本站真实探索，结合时间与评价排序，越近期权重越高。”两个热词区域仍独立。预览仍使用本地验收数据库；未发布。

## 用户执行数据库变更及提交授权
- 用户已在数据库查询入口执行单条 DO 增量脚本，截图确认 public 下三个新表存在且 RLS 均开启。索引、列结构及生产应用连接权限尚未独立核验。
- 仓库 SQL 同步为单条 DO 语句并开启三表 RLS；保留 search_path 以支持隔离 schema 验收，生产执行目标为 public。
- 用户授权提交和推送本次特性分支；未授权合并或部署。
